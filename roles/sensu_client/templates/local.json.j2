{
    "client": {
        "name": "{{ ansible_fqdn }}", 
        "address": "{{ ansible_default_ipv4.address }}", 
        "subscriptions": [
            "host", 
            "host-{{ ansible_hostname }}"
        ]
    }
}
